import styled from "styled-components";

export const Wrapper = styled.div`
    display: flex;
    grid-gap: 5px;
`

export const Input = styled.input`
    flex: 1;
    border: none;
    outline: none;
`
