import styled from "styled-components";

export const Wrapper = styled.div`
    position: relative;
    display: flex;
    padding-right: 42px;
    min-height: 32px;
`

export const Input = styled.input`
    flex: 1;
    border: none;
    outline: none;
`
